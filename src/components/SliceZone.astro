---
import Hero from "./slices/Hero.astro";

const { slices } = Astro.props;
---

{
  slices.map((slice) => {
    switch (slice.slice_type) {
      case "hero": {
        return <Hero {...slice} />;
      }
    }
  })
}
