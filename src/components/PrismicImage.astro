---
import * as prismic from "@prismicio/client";

const { content, className, ...rest } = Astro.props;

const src = prismic.asImageSrc(content);
---

<img src={src} alt={content?.alt} class={className} {...rest} />
