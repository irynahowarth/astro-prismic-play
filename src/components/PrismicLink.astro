---
import * as prismic from "@prismicio/client";
import clsx from "clsx";

const { link, className } = Astro.props;

const url = prismic.asLink(link);
---

<a
  href={url}
  class={clsx("font-display font-bold text-base tracking-wider ", className)}
  ><slot /></a
>
